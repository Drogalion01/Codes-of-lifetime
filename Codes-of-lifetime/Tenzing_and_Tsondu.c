#include<stdio.h>
int main()
{
    int t, m, n, a[50], b[50];
    scanf("%d", &t);
    for(int i=0; i<t; i++)
    {
        scanf("%d %d", &m, &n);
        for(int j=0; j<m; j++)
            scanf("%d", &a[j]);
        for(int k=0; k<n; k++)
            scanf("%d", &a[k]);
        
    }
}